sudo: false

language: php

cache:
  directories:
    - $HOME/.composer/cache/files

php:
  - 5.6
  - 7.0
  - 7.1
  - 7.2

matrix:
  fast_finish: true

install:
  - composer install

script:
  - find -name '*.php' -not -path './vendor/*' -name '*.php' | xargs -n1 php -l
